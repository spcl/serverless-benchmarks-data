The inputs are fetched during execution from other resources: Wikipedia, GitHub, PyTorch website. Here, we stored expected outputs.

* `test` - 960px thumbnail from Wikipedia. Previously, we used 800px thumbnail which is no longer available.
* `small` - HPX 1.4.0 release as source code, from GitHub.
* `larged` - ResNet-50 model weights, from PyTorch website. It is not stored there, as exactly the same copy is stored in the repository as `400.inference/411.image-recognition/model/resnet50-19c8e357.pth`.
